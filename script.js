function converter() {
  var valor_reais = document.getElementById("txtvalor_reais").value;
  var valor_convertido = valor_reais / 5;
  document.getElementById("txtvalor_convertido").value = valor_convertido;
}
